# CLAUDE.md

This directory contains outer page containers.

`public.html` is used by the legacy forum pages. `shim.html` wraps the standalone XenForo chat shim and is part of the active `xf-chat` flow.

When editing containers, preserve required blocks, CSP nonce usage, asset includes, and body classes expected by page templates and frontend styles.
