# CLAUDE.md

This directory contains reusable Askama fragments for user-generated content.

These templates render legacy forum/post/message UGC structures and are mostly outside the active `xf-chat` path. Chat WebSocket messages are rendered client-side from JSON payloads, not through these fragments.

If changing shared UGC markup, check legacy post/thread templates and CSS for class dependencies.
