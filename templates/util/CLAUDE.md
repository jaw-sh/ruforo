# CLAUDE.md

This directory contains small reusable template utilities.

`paginator.html` renders pagination controls for legacy server-rendered pages. Keep utility templates generic and avoid adding route-specific data assumptions here.
